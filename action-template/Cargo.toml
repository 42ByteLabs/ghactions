[package]
name = "action"
version = "0.1.0"

edition = "2021"

[dependencies]
ghactions = {version = "^0.9", features = ["generate", "octocrab"]}
anyhow = "1"
log = "0.4"
tokio = { version = "1.38", default-features = false, features = [
    "macros",
    "rt-multi-thread",
    "time",
] }

