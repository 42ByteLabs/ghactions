# This file is generated by ghactions
# Do not edit this file manually unless you disable the `generate` feature.

name: My Action
description: My Action Description
inputs:
  repo:
    description: Repository Name
    default: ${{ github.repository }}
  token:
    description: GitHub Token
    default: ${{ github.token }}
  mode:
    description: Mode
    default: 'false'
  crates:
    description: Crate names (comma separated)
outputs:
  version:
    description: Output Version
runs:
  using: composite
  image: ./examples/advanced/Dockerfile
  steps:
  - name: Checking for Cargo/Rust
    shell: bash
    run: ''
  - id: cargo-run
    shell: bash
    run: cargo run --target-dir /home/geekmasher/development/42bytelabs/ghactions/ghactions-core
