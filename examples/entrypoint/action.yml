# This file is generated by ghactions
# Do not edit this file manually unless you disable the `generate` feature.

name: Advanced Compile Action
description: Advanced Compile Action Description
inputs:
  crates:
    description: Crate names (comma separated)
outputs:
  version:
    description: Output Version
    value: ${{ steps.entrypoint-script.outputs.random-number }}
runs:
  using: composite
  steps:
  - id: entrypoint-script
    shell: bash
    run: ${{ github.action_path }}/examples/entrypoint/entrypoint.sh
