# This file is generated by ghactions
# Do not edit this file manually unless you disable the `generate` feature.

name: Advanced JIT Action
description: Advanced JIT Action Description
inputs:
  crates:
    description: Crate names (comma separated)
outputs:
  version:
    description: Output Version
    value: ${{ steps.cargo-run.outputs.version }}
runs:
  using: composite
  steps:
  - name: Compile / Install the Action binary
    shell: bash
    run: |-
      set -e
      cargo install --path "${{ github.action_path }}"
  - id: cargo-run
    name: Run the Action
    shell: bash
    run: |-
      set -e
      action
